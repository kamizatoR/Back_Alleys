<div class="row mt-5">
  <div class="col-10 px-5 mt-5 justify-content-center text-center",>

        <%= form_with model: [@post, @comment, @reply], url: update_reply_path(@post.end_user.display_name,@post.id, @comment.id, @reply.id),method: :patch do |f| %>


          <div class="row  mb-3  justify-content-around ">
            <%= f.text_area :body, placeholder: "本文", class: "form-control"  %>
          </div>

          <div class="row mt-3 mb-0  justify-content-around ">
            <%= f.submit "変更内容を保存", class: "btn btn-success col-4" %>
          </div>
        <% end %>


  </div>

  <div class="col-2 pl-0">
    <ul class="list-unstyled">
      <li>
        <%= link_to "削除", destroy_reply_path(@post.end_user.display_name, @post.id, @comment.id, @reply.id), method: :delete, "data-confirm"=> "本当に削除しますか？" %>
    </ul>
  </div>
</div>
