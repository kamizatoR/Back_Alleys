<%= form_with model: Like.new, url: likes_path(display_name: params[:display_name], post_id: params[:id]) do |f| %>
      <div class="col-11">
          <%= f.hidden_field :table_id, value: table.id %>
          <%= f.hidden_field :table_type, value: table.class %>
      </div>
      <div class="text-right">
        <%= f.submit "いいね", class: "btn btn-outline-info" %>
      </div>
<% end %>
