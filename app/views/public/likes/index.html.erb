<div class="container">
  <div class="header">
      <div class="row border-bottom justify-content-center">
        <div class="col-10 text-center ">
          <h3 class=""><%= @end_user.display_name %>さんのいいねリスト</h3>

        </div>
        <div class="col-10">
          <div class="row justify-content-around">
            <div class="col-4 border text-center">
              <%= link_to post_likes_path(@end_user.display_name), class: "text-decoration-none text-dark"  do %>
                投稿
              <% end %>
            </div>
            <div class="col-4 border text-center">
              <%= link_to comment_likes_path(@end_user.display_name), class: "text-decoration-none text-dark"  do %>
                コメント
              <% end %>
            </div>
            <div class="col-4 border text-center">リプライ</div>
          </div>
        </div>


      </div>
    </div>
<% @like.each do |like| %>

  <%= like.table.body %>
  <%= like.table_type %>

<% end %>
</div>